public class WrapperClasses {
    public static void main(String[] args){

        // Wrapper Classes  -   provides a way to use primitive data types sa reference data types 
        //                      reference data types contain useful methods
        //                      can be used with collection (ex.ArrayList)

        /*      Primitive       //      Wrapper
         *   > boolean               > Boolean
         *   > char                  > Character
         *   > int                   > Integer
         *   > double                > Double 
         *
         */

        // autoboxing = the automatic conversion that the Java compiler makes between primitive data types
        //              and their corresponding wrapper class
        // unboxing = the reverse of autoboxing. Automatic conversion of wrapper class to primitive

        // Autoboxing - conversion of primitive data type to wrapper. T
        
        /* 
        Boolean a = true;
        Character b = '@';
        Integer c = 20;
        Double d = 28.32223432432;
        String e = "Rasty";

        if(b=='@'){
            System.out.println("This is true");
        }
        */
        
        // Unboxing - treatment of reference data type as if they're still primitive which holds the actual value

    }
}
