public class WildcardMatching {
    public static void main(String[] args) {
        String s = "adceb";
        String p = "*a*b";

        System.out.println("String s: " + s);
        System.out.println("Pattern p: " + p);
        boolean isMatch = isWildcardMatch(s, p);
        System.out.println("Is Wildcard Match: " + isMatch);
    }

    public static boolean isWildcardMatch(String s, String p) {
        // Implement a method to check if a string matches a wildcard pattern
        return false;
    }
}
