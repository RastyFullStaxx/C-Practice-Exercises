public class CountBinarySubstrings {
    public static void main(String[] args) {
        String s = "00110011";

        System.out.println("String s: " + s);
        int countSubstrings = countBinarySubstrings(s);
        System.out.println("Count of Binary Substrings: " + countSubstrings);
    }

    public static int countBinarySubstrings(String s) {
        // Implement a method to count the number of non-empty (contiguous) substrings with equal number of 0's and 1's
        return 0;
    }
}
