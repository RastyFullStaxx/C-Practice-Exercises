public class UniquePaths {
    public static void main(String[] args) {
        int m = 3, n = 7;

        System.out.println("Grid Dimensions: " + m + "x" + n);
        int uniquePaths = uniquePaths(m, n);
        System.out.println("Number of Unique Paths: " + uniquePaths);
    }

    public static int uniquePaths(int m, int n) {
        // Implement a method to find the number of unique paths from the top-left to the bottom-right corner
        return 0;
    }
}
