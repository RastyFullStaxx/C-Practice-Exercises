public class FindDuplicateNumber {
    public static void main(String[] args) {
        int[] nums = {1, 3, 4, 2, 2};

        System.out.println("Array of Numbers: " + Arrays.toString(nums));
        int duplicateNumber = findDuplicate(nums);
        System.out.println("Duplicate Number: " + duplicateNumber);
    }

    public static int findDuplicate(int[] nums) {
        // Implement a method to find the duplicate number in an array
        return 0;
    }
}
