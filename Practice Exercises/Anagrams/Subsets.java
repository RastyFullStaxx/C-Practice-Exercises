import java.util.List;

public class Subsets {
    public static void main(String[] args) {
        int[] nums = {1, 2, 3};

        System.out.println("Array of Numbers: " + Arrays.toString(nums));
        List<List<Integer>> allSubsets = subsets(nums);
        System.out.println("All Subsets: " + allSubsets);
    }

    public static List<List<Integer>> subsets(int[] nums) {
        // Implement a method to generate all possible subsets of a set of distinct integers
        return null;
    }
}
