public class ProductExceptSelf {
    public static void main(String[] args) {
        int[] nums = {1, 2, 3, 4};

        System.out.println("Array of Numbers: " + Arrays.toString(nums));
        int[] result = productExceptSelf(nums);
        System.out.println("Product Except Self: " + Arrays.toString(result));
    }

    public static int[] productExceptSelf(int[] nums) {
        // Implement a method to calculate the product of the array except self for each element
        return null;
    }
}
